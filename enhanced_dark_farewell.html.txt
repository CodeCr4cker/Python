<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="shortcut icon" href="favicon.png" type="image/png">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no" />
    <title>Farewell Celebration</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Dancing+Script:wght@400;600;700&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Enhanced Dark Theme Variables */
        :root {
            --primary-dark: #0a0a0f;
            --secondary-dark: #121218;
            --accent-dark: #1a1a2e;
            --deep-dark: #0d0d1a;
            --neon-cyan: #00fff5;
            --neon-purple: #bf00ff;
            --neon-pink: #ff0080;
            --electric-blue: #0080ff;
        }

        /* Loader Styles */
        .loader-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--primary-dark) 0%, var(--secondary-dark) 50%, var(--deep-dark) 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            transition: opacity 0.5s ease-out;
        }

        .loader {
            text-align: center;
            color: white;
        }

        .spinner {
            width: 100px;
            height: 100px;
            border: 6px solid rgba(0, 255, 245, 0.1);
            border-top: 6px solid var(--neon-cyan);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 30px;
            box-shadow: 0 0 40px rgba(0, 255, 245, 0.3);
        }

        .loader-text {
            font-family: 'Orbitron', sans-serif;
            font-size: 26px;
            font-weight: 700;
            letter-spacing: 3px;
            animation: pulse 2s ease-in-out infinite;
            background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple), var(--neon-pink));
            background-size: 300% 300%;
            animation: gradientShift 3s ease infinite, pulse 2s ease-in-out infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.7; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.05); }
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Main Content */
        .main-content {
            opacity: 0;
            transition: opacity 0.5s ease-in;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--primary-dark) 0%, var(--secondary-dark) 30%, var(--accent-dark) 70%, var(--deep-dark) 100%);
            min-height: 100vh;
            overflow-x: hidden;
            color: #ffffff;
            position: relative;
        }

        /* Enhanced Background Effects */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 255, 245, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(191, 0, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 60% 20%, rgba(255, 0, 128, 0.08) 0%, transparent 50%);
            z-index: 0;
            pointer-events: none;
        }

        /* Invitation Form Modal */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s ease;
            backdrop-filter: blur(15px);
        }

        .modal-overlay.active {
            opacity: 1;
            pointer-events: all;
        }

        .invitation-form {
            background: linear-gradient(135deg, var(--secondary-dark), var(--accent-dark));
            border: 2px solid var(--neon-cyan);
            padding: 50px;
            border-radius: 25px;
            box-shadow: 
                0 25px 80px rgba(0, 0, 0, 0.7), 
                0 0 60px rgba(0, 255, 245, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
            text-align: center;
            max-width: 450px;
            width: 90%;
            transform: translateY(50px);
            transition: transform 0.3s ease;
            position: relative;
        }

        .modal-overlay.active .invitation-form {
            transform: translateY(0);
        }

        .form-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 36px;
            background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 25px;
            font-weight: 900;
            text-shadow: 0 0 30px rgba(0, 255, 245, 0.5);
        }

        .form-input {
            width: 100%;
            padding: 18px;
            margin: 15px 0;
            border: 2px solid var(--accent-dark);
            border-radius: 15px;
            font-size: 16px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
            background: var(--primary-dark);
            color: #fff;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--neon-cyan);
            box-shadow: 0 0 25px rgba(0, 255, 245, 0.4);
            background: var(--secondary-dark);
        }

        .form-button {
            background: linear-gradient(135deg, var(--neon-cyan), var(--electric-blue));
            color: var(--primary-dark);
            border: none;
            padding: 18px 35px;
            border-radius: 15px;
            font-size: 16px;
            font-weight: 700;
            cursor: pointer;
            margin: 10px 8px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .form-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0, 255, 245, 0.4);
        }

        /* Enhanced Particle Animation */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: var(--neon-cyan);
            border-radius: 50%;
            animation: float 8s ease-in-out infinite;
            box-shadow: 0 0 15px rgba(0, 255, 245, 0.8);
        }

        .particle.purple {
            background: var(--neon-purple);
            box-shadow: 0 0 15px rgba(191, 0, 255, 0.8);
        }

        .particle.pink {
            background: var(--neon-pink);
            box-shadow: 0 0 15px rgba(255, 0, 128, 0.8);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg) scale(1); opacity: 0.8; }
            25% { transform: translateY(-40px) rotate(90deg) scale(1.2); opacity: 1; }
            50% { transform: translateY(-60px) rotate(180deg) scale(0.8); opacity: 0.9; }
            75% { transform: translateY(-30px) rotate(270deg) scale(1.1); opacity: 1; }
        }

        /* Header Decorations */
        .decoration {
            position: absolute;
            z-index: 2;
        }

        .top-right-decoration {
            top: 30px;
            right: 30px;
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, rgba(0, 255, 245, 0.1), rgba(191, 0, 255, 0.1));
            border: 3px solid var(--neon-cyan);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            color: var(--neon-cyan);
            animation: glow-pulse 3s ease-in-out infinite alternate;
        }

        .top-left-decoration {
            top: 30px;
            left: 30px;
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, rgba(255, 0, 128, 0.1), rgba(0, 128, 255, 0.1));
            border: 3px solid var(--neon-pink);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            color: var(--neon-pink);
            animation: glow-pulse 3s ease-in-out infinite alternate;
        }

        @keyframes glow-pulse {
            0% { 
                box-shadow: 0 0 25px rgba(0, 255, 245, 0.3);
                transform: scale(1);
            }
            100% { 
                box-shadow: 0 0 45px rgba(0, 255, 245, 0.7);
                transform: scale(1.05);
            }
        }

        /* Main Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 3;
        }

        /* Title Section */
        .title-section {
            text-align: center;
            margin: 100px 0 80px;
            position: relative;
        }

        .main-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 5rem;
            font-weight: 900;
            background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple), var(--neon-pink), var(--electric-blue));
            background-size: 400% 400%;
            animation: gradientShift 5s ease infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 15px;
            text-shadow: 0 0 80px rgba(0, 255, 245, 0.6);
            letter-spacing: 5px;
        }

        .sub-title {
            font-family: 'Dancing Script', cursive;
            font-size: 3rem;
            color: #ffffff;
            margin-bottom: 40px;
            font-weight: 700;
            text-shadow: 0 0 40px rgba(255, 255, 255, 0.4);
        }

        .event-details {
            background: linear-gradient(135deg, rgba(10, 10, 15, 0.8), rgba(26, 26, 46, 0.8));
            backdrop-filter: blur(20px);
            border-radius: 25px;
            padding: 35px;
            margin: 30px 0;
            border: 2px solid rgba(0, 255, 245, 0.3);
            box-shadow: 
                0 0 40px rgba(0, 255, 245, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
        }

        .event-details p {
            color: rgba(255, 255, 255, 0.95);
            font-size: 20px;
            font-weight: 500;
            line-height: 1.8;
        }

        .highlight {
            color: var(--neon-cyan);
            font-weight: 700;
            text-shadow: 0 0 15px rgba(0, 255, 245, 0.6);
        }

        /* REVOLUTIONARY NEON COUNTDOWN DESIGN */
        .countdown-container {
            display: flex;
            justify-content: center;
            margin: 80px 0;
            perspective: 2000px;
            position: relative;
        }

        /* Ambient glow around countdown */
        .countdown-container::before {
            content: '';
            position: absolute;
            top: -50px;
            left: -50px;
            right: -50px;
            bottom: -50px;
            background: radial-gradient(ellipse at center, rgba(0, 255, 245, 0.15) 0%, rgba(191, 0, 255, 0.1) 50%, transparent 70%);
            border-radius: 50px;
            animation: ambientPulse 4s ease-in-out infinite alternate;
            z-index: -2;
        }

        @keyframes ambientPulse {
            0% { transform: scale(1); opacity: 0.3; }
            100% { transform: scale(1.1); opacity: 0.7; }
        }

        .countdown {
            background: linear-gradient(135deg, rgba(10, 10, 15, 0.95), rgba(26, 26, 46, 0.95));
            backdrop-filter: blur(30px);
            border-radius: 35px;
            padding: 60px 40px;
            border: 3px solid transparent;
            background-clip: padding-box;
            box-shadow: 
                0 30px 80px rgba(0, 0, 0, 0.9), 
                0 0 120px rgba(0, 255, 245, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.15),
                inset 0 -1px 0 rgba(0, 255, 245, 0.1);
            position: relative;
            overflow: hidden;
            transform: rotateX(5deg);
            transition: transform 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .countdown:hover {
            transform: rotateX(0deg) translateY(-10px);
            box-shadow: 
                0 40px 100px rgba(0, 0, 0, 0.9), 
                0 0 150px rgba(0, 255, 245, 0.6),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }

        /* Multi-layered animated border gradient */
        .countdown::before {
            content: '';
            position: absolute;
            inset: 0;
            padding: 3px;
            background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple), var(--neon-pink), var(--electric-blue), var(--neon-cyan));
            background-size: 400% 400%;
            border-radius: 35px;
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: xor;
            animation: neonBorderFlow 6s linear infinite;
            z-index: -1;
        }

        @keyframes neonBorderFlow {
            0% { background-position: 0% 50%; }
            25% { background-position: 100% 50%; }
            50% { background-position: 100% 100%; }
            75% { background-position: 0% 100%; }
            100% { background-position: 0% 50%; }
        }

        /* Multiple floating energy orbs */
        .countdown::after {
            content: '';
            position: absolute;
            width: 150px;
            height: 150px;
            background: radial-gradient(circle, rgba(0, 255, 245, 0.2) 0%, rgba(191, 0, 255, 0.1) 50%, transparent 70%);
            border-radius: 50%;
            top: -30px;
            right: -30px;
            animation: orbFloat1 10s ease-in-out infinite;
            z-index: -1;
        }

        /* Additional energy orbs */
        .countdown-energy-orb1,
        .countdown-energy-orb2,
        .countdown-energy-orb3 {
            position: absolute;
            border-radius: 50%;
            animation: orbFloat2 8s ease-in-out infinite;
            z-index: -1;
            pointer-events: none;
        }

        .countdown-energy-orb1 {
            width: 100px;
            height: 100px;
            background: radial-gradient(circle, rgba(255, 0, 128, 0.15) 0%, transparent 70%);
            top: 20px;
            left: -20px;
            animation-delay: -2s;
            animation-duration: 12s;
        }

        .countdown-energy-orb2 {
            width: 80px;
            height: 80px;
            background: radial-gradient(circle, rgba(0, 128, 255, 0.15) 0%, transparent 70%);
            bottom: -10px;
            right: 50px;
            animation-delay: -4s;
            animation-duration: 14s;
        }

        .countdown-energy-orb3 {
            width: 120px;
            height: 120px;
            background: radial-gradient(circle, rgba(191, 0, 255, 0.12) 0%, transparent 70%);
            top: 50%;
            left: -40px;
            animation-delay: -6s;
            animation-duration: 16s;
        }

        @keyframes orbFloat1 {
            0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
            33% { transform: translate(-40px, -30px) scale(1.3) rotate(120deg); opacity: 1; }
            66% { transform: translate(20px, -50px) scale(0.9) rotate(240deg); opacity: 0.9; }
        }

        @keyframes orbFloat2 {
            0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.7; }
            25% { transform: translate(30px, 20px) scale(1.2) rotate(90deg); opacity: 1; }
            50% { transform: translate(-20px, 40px) scale(0.8) rotate(180deg); opacity: 0.8; }
            75% { transform: translate(10px, -30px) scale(1.1) rotate(270deg); opacity: 0.9; }
        }

        .countdown-title {
            text-align: center;
            font-family: 'Orbitron', sans-serif;
            font-size: 32px;
            font-weight: 900;
            background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple), var(--neon-pink), var(--electric-blue));
            background-size: 300% 300%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 50px;
            text-transform: uppercase;
            letter-spacing: 6px;
            animation: titleGradientFlow 4s ease-in-out infinite, titleGlow 2s ease-in-out infinite alternate;
            position: relative;
        }

        /* Holographic underline effect */
        .countdown-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 200px;
            height: 4px;
            background: linear-gradient(90deg, transparent, var(--neon-cyan), var(--neon-purple), var(--neon-cyan), transparent);
            border-radius: 2px;
            animation: underlineFlow 3s ease-in-out infinite;
        }

        @keyframes titleGradientFlow {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        @keyframes titleGlow {
            0% { text-shadow: 0 0 20px rgba(0, 255, 245, 0.5), 0 0 40px rgba(0, 255, 245, 0.3); }
            100% { text-shadow: 0 0 30px rgba(0, 255, 245, 0.8), 0 0 60px rgba(191, 0, 255, 0.4); }
        }

        @keyframes underlineFlow {
            0%, 100% { transform: translateX(-50%) scaleX(0.5); opacity: 0.7; }
            50% { transform: translateX(-50%) scaleX(1.2); opacity: 1; }
        }

        .countdown-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 35px;
            max-width: 900px;
            position: relative;
        }

        /* Grid connection lines */
        .countdown-grid::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 245, 0.3), rgba(191, 0, 255, 0.3), rgba(0, 255, 245, 0.3), transparent);
            transform: translateY(-50%);
            animation: connectionPulse 3s ease-in-out infinite;
            z-index: -1;
        }

        @keyframes connectionPulse {
            0%, 100% { opacity: 0.3; transform: translateY(-50%) scaleX(0.8); }
            50% { opacity: 1; transform: translateY(-50%) scaleX(1.1); }
        }

        /* FUTURISTIC NEON COUNTDOWN ITEMS */
        .countdown-item {
            text-align: center;
            color: white;
            padding: 45px 25px;
            border-radius: 30px;
            background: linear-gradient(135deg, rgba(0, 255, 245, 0.05), rgba(191, 0, 255, 0.05), rgba(255, 0, 128, 0.05));
            border: 3px solid transparent;
            background-clip: padding-box;
            position: relative;
            transition: all 0.7s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            overflow: hidden;
            transform-style: preserve-3d;
        }

        /* Dynamic hexagonal border pattern */
        .countdown-item::before {
            content: '';
            position: absolute;
            inset: 0;
            padding: 3px;
            background: linear-gradient(45deg, transparent, var(--neon-cyan), transparent, var(--neon-purple), transparent);
            background-size: 400% 400%;
            border-radius: 30px;
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: xor;
            opacity: 0;
            transition: all 0.5s ease;
            z-index: -1;
            animation: borderIdle 8s ease-in-out infinite;
        }

        @keyframes borderIdle {
            0%, 100% { background-position: 0% 50%; opacity: 0.4; }
            25% { background-position: 100% 50%; opacity: 0.6; }
            50% { background-position: 100% 100%; opacity: 0.8; }
            75% { background-position: 0% 100%; opacity: 0.5; }
        }

        /* Individual item color themes */
        .countdown-item:nth-child(1)::before {
            background: linear-gradient(45deg, transparent, var(--neon-cyan), var(--electric-blue), var(--neon-cyan), transparent);
        }

        .countdown-item:nth-child(2)::before {
            background: linear-gradient(45deg, transparent, var(--neon-purple), var(--neon-cyan), var(--neon-purple), transparent);
        }

        .countdown-item:nth-child(3)::before {
            background: linear-gradient(45deg, transparent, var(--neon-pink), var(--neon-purple), var(--neon-pink), transparent);
        }

        .countdown-item:nth-child(4)::before {
            background: linear-gradient(45deg, transparent, var(--electric-blue), var(--neon-pink), var(--electric-blue), transparent);
        }

        /* Enhanced hover effects */
        .countdown-item:hover::before {
            opacity: 1;
            animation: borderSpin 3s linear infinite, borderPulse 1.5s ease-in-out infinite alternate;
        }

        @keyframes borderSpin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        @keyframes borderPulse {
            0% { filter: brightness(1); }
            100% { filter: brightness(1.5) saturate(1.3); }
        }

        /* Multi-layer holographic sweep */
        .countdown-item::after {
            content: '';
            position: absolute;
            top: 0;
            left: -200%;
            width: 200%;
            height: 100%;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 25%, 
                rgba(0, 255, 245, 0.3) 50%, 
                rgba(255, 255, 255, 0.1) 75%, 
                transparent 100%);
            transition: left 1.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border-radius: 30px;
            z-index: 1;
        }

        /* Advanced hover transformations */
        .countdown-item:hover {
            transform: translateY(-20px) rotateY(12deg) rotateX(8deg) scale(1.08);
            box-shadow: 
                0 30px 60px rgba(0, 0, 0, 0.6),
                0 0 80px rgba(0, 255, 245, 0.5),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
            background: linear-gradient(135deg, rgba(0, 255, 245, 0.1), rgba(191, 0, 255, 0.1), rgba(255, 0, 128, 0.1));
        }

        .countdown-item:hover::after {
            left: 200%;
        }

        /* Energy particle effect on hover */
        .countdown-item:hover .countdown-number::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 6px;
            height: 6px;
            background: var(--neon-cyan);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            box-shadow: 
                0 0 20px var(--neon-cyan),
                20px 20px 0 var(--neon-purple),
                -20px -20px 0 var(--neon-pink),
                20px -20px 0 var(--electric-blue),
                -20px 20px 0 var(--neon-cyan);
            animation: energyParticles 2s ease-in-out infinite;
            z-index: -1;
        }

        @keyframes energyParticles {
            0%, 100% { 
                transform: translate(-50%, -50%) scale(0) rotate(0deg); 
                opacity: 0;
            }
            50% { 
                transform: translate(-50%, -50%) scale(1) rotate(180deg); 
                opacity: 1;
            }
        }

        /* SPECTACULAR NEON NUMBERS */
        .countdown-number {
            font-family: 'Orbitron', sans-serif;
            font-size: 4.5rem;
            font-weight: 900;
            display: block;
            margin-bottom: 15px;
            background: linear-gradient(45deg, var(--neon-cyan), #ffffff, var(--electric-blue));
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            z-index: 2;
            animation: numberShimmer 3s ease-in-out infinite alternate;
        }

        @keyframes numberShimmer {
            0% { 
                background-position: 0% 50%;
                text-shadow: 
                    0 0 20px rgba(0, 255, 245, 0.8), 
                    0 0 40px rgba(0, 255, 245, 0.6),
                    0 0 60px rgba(0, 255, 245, 0.4);
                filter: brightness(1);
            }
            100% { 
                background-position: 100% 50%;
                text-shadow: 
                    0 0 30px rgba(0, 255, 245, 1), 
                    0 0 60px rgba(0, 255, 245, 0.8),
                    0 0 90px rgba(0, 255, 245, 0.6);
                filter: brightness(1.3);
            }
        }

        /* Individual number color schemes with dynamic backgrounds */
        .countdown-item:nth-child(1) .countdown-number {
            background: linear-gradient(45deg, var(--neon-cyan), #ffffff, var(--electric-blue), var(--neon-cyan));
            background-size: 300% 300%;
            animation: numberShimmer1 4s ease-in-out infinite alternate;
        }

        .countdown-item:nth-child(2) .countdown-number {
            background: linear-gradient(45deg, var(--neon-purple), #ffffff, var(--neon-cyan), var(--neon-purple));
            background-size: 300% 300%;
            animation: numberShimmer2 3.5s ease-in-out infinite alternate;
        }

        .countdown-item:nth-child(3) .countdown-number {
            background: linear-gradient(45deg, var(--neon-pink), #ffffff, var(--neon-purple), var(--neon-pink));
            background-size: 300% 300%;
            animation: numberShimmer3 4.5s ease-in-out infinite alternate;
        }

        .countdown-item:nth-child(4) .countdown-number {
            background: linear-gradient(45deg, var(--electric-blue), #ffffff, var(--neon-pink), var(--electric-blue));
            background-size: 300% 300%;
            animation: numberShimmer4 2.8s ease-in-out infinite alternate;
        }

        @keyframes numberShimmer1 {
            0% { 
                background-position: 0% 50%;
                text-shadow: 0 0 30px rgba(0, 255, 245, 0.8), 0 0 60px rgba(0, 255, 245, 0.5);
            }
            100% { 
                background-position: 100% 50%;
                text-shadow: 0 0 40px rgba(0, 255, 245, 1), 0 0 80px rgba(0, 128, 255, 0.7);
            }
        }

        @keyframes numberShimmer2 {
            0% { 
                background-position: 0% 50%;
                text-shadow: 0 0 30px rgba(191, 0, 255, 0.8), 0 0 60px rgba(191, 0, 255, 0.5);
            }
            100% { 
                background-position: 100% 50%;
                text-shadow: 0 0 40px rgba(191, 0, 255, 1), 0 0 80px rgba(0, 255, 245, 0.7);
            }
        }

        @keyframes numberShimmer3 {
            0% { 
                background-position: 0% 50%;
                text-shadow: 0 0 30px rgba(255, 0, 128, 0.8), 0 0 60px rgba(255, 0, 128, 0.5);
            }
            100% { 
                background-position: 100% 50%;
                text-shadow: 0 0 40px rgba(255, 0, 128, 1), 0 0 80px rgba(191, 0, 255, 0.7);
            }
        }

        @keyframes numberShimmer4 {
            0% { 
                background-position: 0% 50%;
                text-shadow: 0 0 30px rgba(0, 128, 255, 0.8), 0 0 60px rgba(0, 128, 255, 0.5);
            }
            100% { 
                background-position: 100% 50%;
                text-shadow: 0 0 40px rgba(0, 128, 255, 1), 0 0 80px rgba(255, 0, 128, 0.7);
            }
        }

        /* Enhanced hover effects for numbers */
        .countdown-item:hover .countdown-number {
            transform: scale(1.15) rotateX(10deg);
            filter: brightness(1.5) saturate(1.4);
            text-shadow: 
                0 0 50px rgba(0, 255, 245, 1),
                0 0 100px rgba(191, 0, 255, 0.8),
                0 0 150px rgba(255, 0, 128, 0.6);
            animation-duration: 0.8s;
        }

        /* Digital glitch effect for numbers */
        .countdown-number::after {
            content: attr(data-text);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: inherit;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            opacity: 0;
            animation: digitalGlitch 6s linear infinite;
        }

        @keyframes digitalGlitch {
            0%, 96%, 100% { opacity: 0; transform: translateX(0); }
            97% { opacity: 0.8; transform: translateX(2px); filter: hue-rotate(90deg); }
            98% { opacity: 0.6; transform: translateX(-2px); filter: hue-rotate(180deg); }
            99% { opacity: 0.9; transform: translateX(1px); filter: hue-rotate(270deg); }
        }

        .countdown-label {
            font-family: 'Orbitron', sans-serif;
            font-size: 18px;
            font-weight: 700;
            opacity: 0.95;
            text-transform: uppercase;
            letter-spacing: 4px;
            color: #ffffff;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.4);
            position: relative;
            z-index: 2;
            transition: all 0.3s ease;
        }

        .countdown-item:hover .countdown-label {
            color: var(--neon-cyan);
            text-shadow: 0 0 25px rgba(0, 255, 245, 0.8);
            transform: translateY(-5px);
        }

        /* Enhanced pulse effect for seconds with energy waves */
        .countdown-seconds {
            position: relative;
        }

        .countdown-seconds::before {
            content: '';
            position: absolute;
            inset: -10px;
            border: 2px solid var(--electric-blue);
            border-radius: 35px;
            opacity: 0;
            animation: secondsEnergyWave 1s ease-out infinite;
        }

        .countdown-seconds::after {
            content: '';
            position: absolute;
            inset: -20px;
            border: 1px solid var(--neon-pink);
            border-radius: 40px;
            opacity: 0;
            animation: secondsEnergyWave 1s ease-out infinite 0.3s;
        }

        @keyframes secondsEnergyWave {
            0% { 
                opacity: 0; 
                transform: scale(0.8); 
                border-color: var(--electric-blue);
            }
            30% { 
                opacity: 0.8; 
                transform: scale(1); 
                border-color: var(--neon-cyan);
            }
            70% { 
                opacity: 0.4; 
                transform: scale(1.1); 
                border-color: var(--neon-purple);
            }
            100% { 
                opacity: 0; 
                transform: scale(1.3); 
                border-color: var(--neon-pink);
            }
        }

        .countdown-seconds .countdown-number {
            animation: numberPulse 1s ease-in-out infinite, numberShimmer4 2.8s ease-in-out infinite alternate;
        }

        @keyframes numberPulse {
            0%, 100% { 
                transform: scale(1); 
                filter: brightness(1); 
            }
            50% { 
                transform: scale(1.08); 
                filter: brightness(1.4) saturate(1.2);
                text-shadow: 
                    0 0 40px rgba(0, 128, 255, 1),
                    0 0 80px rgba(255, 0, 128, 0.8),
                    0 0 120px rgba(0, 255, 245, 0.6);
            }
        }

        /* Countdown completion animation */
        .countdown-completed {
            animation: completionCelebration 2s ease-in-out infinite;
        }

        @keyframes completionCelebration {
            0%, 100% { 
                transform: rotateX(5deg) scale(1);
                box-shadow: 0 30px 80px rgba(0, 0, 0, 0.9), 0 0 120px rgba(0, 255, 245, 0.4);
            }
            25% { 
                transform: rotateX(0deg) scale(1.02) rotateZ(1deg);
                box-shadow: 0 35px 90px rgba(0, 0, 0, 0.9), 0 0 150px rgba(191, 0, 255, 0.6);
            }
            50% { 
                transform: rotateX(-2deg) scale(1.05) rotateZ(0deg);
                box-shadow: 0 40px 100px rgba(0, 0, 0, 0.9), 0 0 180px rgba(255, 0, 128, 0.8);
            }
            75% { 
                transform: rotateX(2deg) scale(1.02) rotateZ(-1deg);
                box-shadow: 0 35px 90px rgba(0, 0, 0, 0.9), 0 0 150px rgba(0, 128, 255, 0.6);
            }
        }

        /* Action Buttons */
        .actions-container {
            display: flex;
            justify-content: center;
            gap: 25px;
            margin: 60px 0;
            flex-wrap: wrap;
        }

        .action-btn {
            background: linear-gradient(135deg, rgba(10, 10, 15, 0.8), rgba(26, 26, 46, 0.8));
            backdrop-filter: blur(15px);
            color: white;
            text-decoration: none;
            padding: 20px 30px;
            border-radius: 50px;
            border: 2px solid var(--neon-cyan);
            font-weight: 600;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex;
            align-items: center;
            gap: 12px;
            position: relative;
            overflow: hidden;
            font-family: 'Poppins', sans-serif;
        }

        .action-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 245, 0.3), transparent);
            transition: left 0.6s ease;
        }

        .action-btn:hover::before {
            left: 100%;
        }

        .action-btn:hover {
            background: linear-gradient(135deg, rgba(0, 255, 245, 0.2), rgba(191, 0, 255, 0.2));
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 40px rgba(0, 255, 245, 0.4);
            border-color: var(--neon-purple);
        }

        .invitation-btn {
            background: linear-gradient(135deg, var(--neon-cyan), var(--electric-blue));
            color: var(--primary-dark);
            font-weight: 700;
            border: 2px solid var(--neon-cyan);
        }

        .invitation-btn:hover {
            background: linear-gradient(135deg, var(--neon-purple), var(--neon-pink));
            box-shadow: 0 15px 40px rgba(0, 255, 245, 0.5);
        }

        /* Footer */
        .footer {
            text-align: center;
            margin: 80px 0 50px;
            padding: 40px;
            background: linear-gradient(135deg, rgba(10, 10, 15, 0.8), rgba(26, 26, 46, 0.8));
            backdrop-filter: blur(20px);
            border-radius: 25px;
            border: 2px solid rgba(0, 255, 245, 0.3);
            box-shadow: 0 0 50px rgba(0, 255, 245, 0.2);
        }

        .footer-text {
            color: rgba(255, 255, 255, 0.95);
            font-size: 20px;
            line-height: 1.8;
            margin-bottom: 20px;
        }

        .contact-link {
            color: var(--neon-cyan);
            text-decoration: none;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .contact-link:hover {
            color: var(--neon-purple);
            text-shadow: 0 0 15px rgba(191, 0, 255, 0.6);
        }

        .social-icons {
            margin-top: 30px;
        }

        .social-icon {
            display: inline-block;
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, rgba(0, 255, 245, 0.1), rgba(191, 0, 255, 0.1));
            border: 2px solid var(--neon-cyan);
            color: var(--neon-cyan);
            text-align: center;
            line-height: 46px;
            border-radius: 50%;
            margin: 0 15px;
            transition: all 0.4s ease;
            text-decoration: none;
            font-size: 18px;
        }

        .social-icon:hover {
            background: var(--neon-cyan);
            color: var(--primary-dark);
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 15px 30px rgba(0, 255, 245, 0.4);
        }

        /* Success message */
        .success-message {
            background: linear-gradient(135deg, var(--neon-cyan), var(--electric-blue));
            color: var(--primary-dark);
            padding: 20px;
            border-radius: 15px;
            margin: 25px 0;
            text-align: center;
            display: none;
            box-shadow: 0 0 30px rgba(0, 255, 245, 0.4);
            font-weight: 600;
        }

        /* Enhanced music note animation */
        .music-note {
            position: fixed;
            font-size: 24px;
            color: rgba(0, 255, 245, 0.8);
            pointer-events: none;
            z-index: 2;
            animation: musicFloat 6s ease-in-out infinite;
            text-shadow: 0 0 20px rgba(0, 255, 245, 0.6);
        }

        @keyframes musicFloat {
            0%, 100% { 
                transform: translateY(0) rotate(0deg) scale(1); 
                opacity: 0.8;
            }
            25% { 
                transform: translateY(-50px) rotate(90deg) scale(1.3); 
                opacity: 1;
            }
            50% { 
                transform: translateY(-80px) rotate(180deg) scale(0.9); 
                opacity: 0.9;
            }
            75% { 
                transform: translateY(-40px) rotate(270deg) scale(1.2); 
                opacity: 1;
            }
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }

            .main-title {
                font-size: 3rem;
            }

            .sub-title {
                font-size: 2rem;
            }

            .countdown-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 20px;
            }

            .countdown-number {
                font-size: 2.5rem;
            }

            .actions-container {
                flex-direction: column;
                align-items: center;
            }

            .action-btn {
                width: 100%;
                max-width: 350px;
                justify-content: center;
            }

            .decoration {
                display: none;
            }

            .invitation-form {
                padding: 40px 25px;
            }
        }

        @media (max-width: 480px) {
            .main-title {
                font-size: 2.2rem;
            }

            .sub-title {
                font-size: 1.6rem;
            }

            .countdown-grid {
                grid-template-columns: 1fr 1fr;
                gap: 15px;
            }

            .countdown-item {
                padding: 25px 15px;
            }

            .countdown-number {
                font-size: 2rem;
            }

            .countdown-label {
                font-size: 14px;
            }

            .countdown-title {
                font-size: 20px;
            }
        }

        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: var(--primary-dark);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(var(--neon-cyan), var(--neon-purple));
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(var(--neon-purple), var(--neon-pink));
        }
    </style>
</head>
<body>
    <!-- Loader -->
    <div class="loader-container" id="loader">
        <div class="loader">
            <div class="spinner"></div>
            <div class="loader-text">Loading Your Invitation...</div>
        </div>
    </div>

    <!-- Invitation Form Modal -->
    <div class="modal-overlay" id="invitationModal">
        <div class="invitation-form">
            <h2 class="form-title">Personal Invitation</h2>
            <p style="color: #ccc; margin-bottom: 25px; font-size: 18px;">Enter your details to download your personalized invitation</p>
            <input type="text" class="form-input" id="guestName" placeholder="Enter your full name" maxlength="50">
            <input type="text" class="form-input" id="guestCode" placeholder="Enter 4-digit code" maxlength="4" pattern="[0-9]{4}">
            <div class="success-message" id="successMessage">
                Invitation downloaded successfully!
            </div>
            <div style="margin-top: 25px;">
                <button class="form-button" onclick="downloadInvitation()">Download Invitation</button>
                <button class="form-button" onclick="closeModal()" style="background: linear-gradient(135deg, #444, #666);">Close</button>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main-content" id="mainContent">
        <!-- Particles Background -->
        <div class="particles" id="particles"></div>

        <!-- Music Notes -->
        <div class="music-notes"></div>

        <!-- Decorations -->
        <div class="decoration top-right-decoration">
            <i class="fas fa-star"></i>
        </div>
        <div class="decoration top-left-decoration">
            <i class="fas fa-heart"></i>
        </div>

        <div class="container">
            <!-- Title Section -->
            <div class="title-section">
                <h1 class="main-title">Farewell</h1>
                <h2 class="sub-title">Celebration</h2>
                <div class="event-details">
                    <p>Join us for an unforgettable evening on <span class="highlight">29 November 2025</span></p>
                    <p>at <span class="highlight">Ashirwad Garden</span>, Ratu, Ranchi</p>
                    <p style="font-style: italic; margin-top: 20px; font-size: 18px;">Dinner & Dancing to Follow</p>
                </div>
            </div>

            <!-- Revolutionary Countdown Timer -->
            <div class="countdown-container">
                <div class="countdown">
                    <div class="countdown-title">Event Countdown</div>
                    <div class="countdown-grid" id="countdown">
                        <!-- Countdown will be populated by JavaScript -->
                    </div>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="actions-container">
                <a href="#" class="action-btn invitation-btn" onclick="openInvitationModal()">
                    <i class="fas fa-download"></i>
                    Get Personal Invitation
                </a>
                <a href="https://goo.gl/maps/5z5xX2hTYzU8VGEJ9" target="_blank" class="action-btn">
                    <i class="fas fa-map-marker-alt"></i>
                    View Venue
                </a>
                <a href="https://github.com/vinitshahdeo/vinitshahdeo/raw/master/docs/Sonali%20%26%20Gagan.pdf" class="action-btn" download>
                    <i class="fas fa-file-pdf"></i>
                    Download General Invite
                </a>
            </div>

            <!-- Footer -->
            <div class="footer">
                <p class="footer-text">
                    Can't wait to make this farewell a memorable moment with you!<br>
                    Just a ping away for any queries: <a href="https://its-divyanshu.netlify.app/" class="contact-link">Contact Me</a>
                </p>
                <div class="social-icons">
                    <a href="https://twitter.com/Vinit_Shahdeo" target="_blank" class="social-icon">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a href="#" class="social-icon">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a href="#" class="social-icon">
                        <i class="fab fa-facebook"></i>
                    </a>
                </div>
                <p style="color: rgba(255,255,255,0.8); margin-top: 25px; font-size: 18px;">
                    Join us in our happiness! 💫
                </p>
            </div>
        </div>

        <!-- Hidden audio element -->
        <audio id="backgroundMusic" loop>
            <source src="./assets/mp3/song.mp3" type="audio/mpeg">
        </audio>
    </div>

    <script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js'></script>
    <script>
        // Guest database (in real application, this would be stored securely on server)
        const guestDatabase = {
            "1234": { name: "John Doe", photo: "https://via.placeholder.com/400x600/00fff5/000000?text=John+Doe+Invitation" },
            "5678": { name: "Jane Smith", photo: "https://via.placeholder.com/400x600/bf00ff/ffffff?text=Jane+Smith+Invitation" },
            "9999": { name: "Guest User", photo: "https://via.placeholder.com/400x600/ff0080/ffffff?text=Special+Guest+Invitation" }
            // Add more guests as needed
        };

        // Loader functionality
        window.addEventListener('load', function() {
            setTimeout(() => {
                document.getElementById('loader').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('loader').style.display = 'none';
                    document.getElementById('mainContent').style.opacity = '1';
                    startAnimations();
                }, 500);
            }, 2500);
        });

        // Start animations after loader
        function startAnimations() {
            createParticles();
            createMusicNotes();
            startCountdown();
        }

        // Create enhanced floating particles
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const colors = ['#00fff5', '#bf00ff', '#ff0080', '#0080ff'];
            const classes = ['', 'purple', 'pink', ''];
            
            for (let i = 0; i < 80; i++) {
                const particle = document.createElement('div');
                particle.className = `particle ${classes[Math.floor(Math.random() * classes.length)]}`;
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 8 + 's';
                particle.style.animationDuration = (Math.random() * 4 + 6) + 's';
                particlesContainer.appendChild(particle);
            }
        }

        // Create enhanced floating music notes
        function createMusicNotes() {
            const notes = ['♪', '♫', '♬', '♩', '♭', '♯'];
            const colors = ['#00fff5', '#bf00ff', '#ff0080', '#0080ff'];
            
            setInterval(() => {
                const note = document.createElement('div');
                note.className = 'music-note';
                note.innerHTML = notes[Math.floor(Math.random() * notes.length)];
                note.style.left = Math.random() * 100 + '%';
                note.style.top = '100%';
                note.style.color = colors[Math.floor(Math.random() * colors.length)];
                note.style.animationDuration = (Math.random() * 2 + 4) + 's';
                document.body.appendChild(note);
                
                setTimeout(() => {
                    note.remove();
                }, 6000);
            }, 1500);
        }

        // Revolutionary Countdown Timer with enhanced neon animations
        function startCountdown() {
            const countDownDate = new Date("Nov 29, 2025 18:00:00").getTime();
            
            // Add energy orbs to countdown container
            const countdownContainer = document.querySelector('.countdown');
            if (countdownContainer && !countdownContainer.querySelector('.countdown-energy-orb1')) {
                const orb1 = document.createElement('div');
                orb1.className = 'countdown-energy-orb1';
                const orb2 = document.createElement('div');
                orb2.className = 'countdown-energy-orb2';
                const orb3 = document.createElement('div');
                orb3.className = 'countdown-energy-orb3';
                
                countdownContainer.appendChild(orb1);
                countdownContainer.appendChild(orb2);
                countdownContainer.appendChild(orb3);
            }
            
            const timer = setInterval(function() {
                const now = new Date().getTime();
                const distance = countDownDate - now;
                
                const days = Math.floor(distance / (1000 * 60 * 60 * 24));
                const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((distance % (1000 * 60)) / 1000);
                
                document.getElementById("countdown").innerHTML = `
                    <div class="countdown-item">
                        <span class="countdown-number" data-text="${days.toString().padStart(2, '0')}">${days.toString().padStart(2, '0')}</span>
                        <span class="countdown-label">Days</span>
                    </div>
                    <div class="countdown-item">
                        <span class="countdown-number" data-text="${hours.toString().padStart(2, '0')}">${hours.toString().padStart(2, '0')}</span>
                        <span class="countdown-label">Hours</span>
                    </div>
                    <div class="countdown-item">
                        <span class="countdown-number" data-text="${minutes.toString().padStart(2, '0')}">${minutes.toString().padStart(2, '0')}</span>
                        <span class="countdown-label">Minutes</span>
                    </div>
                    <div class="countdown-item countdown-seconds">
                        <span class="countdown-number" data-text="${seconds.toString().padStart(2, '0')}">${seconds.toString().padStart(2, '0')}</span>
                        <span class="countdown-label">Seconds</span>
                    </div>
                `;
                
                // Enhanced special effects for low numbers
                const countdownElement = document.querySelector('.countdown');
                if (days < 7) {
                    document.querySelector('.countdown-item:first-child').style.animation = 'numberPulse 2s ease-in-out infinite, borderPulse 1.5s ease-in-out infinite alternate';
                }
                if (hours < 12) {
                    document.querySelector('.countdown-item:nth-child(2)').style.animation = 'numberPulse 1.8s ease-in-out infinite, borderPulse 1.3s ease-in-out infinite alternate';
                }
                if (minutes < 30) {
                    document.querySelector('.countdown-item:nth-child(3)').style.animation = 'numberPulse 1.5s ease-in-out infinite, borderPulse 1.2s ease-in-out infinite alternate';
                }
                
                // Add intense effects when time is very low
                if (distance < 3600000) { // Less than 1 hour
                    countdownElement.classList.add('countdown-urgent');
                    countdownElement.style.animation = 'completionCelebration 3s ease-in-out infinite';
                }
                
                if (distance < 0) {
                    clearInterval(timer);
                    countdownElement.classList.add('countdown-completed');
                    document.getElementById("countdown").innerHTML = `
                        <div style="text-align: center; color: #00fff5; font-size: 32px; padding: 40px; grid-column: 1 / -1; font-family: 'Orbitron', sans-serif; 
                                    background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple), var(--neon-pink), var(--electric-blue));
                                    background-size: 400% 400%; -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
                                    animation: titleGradientFlow 2s ease-in-out infinite;
                                    text-shadow: 0 0 50px rgba(0, 255, 245, 0.8);">
                            🎉 THE CELEBRATION HAS BEGUN! 🎉<br>
                            <span style="font-size: 24px; margin-top: 20px; display: block;">Let's make unforgettable memories!</span>
                        </div>
                    `;
                }
            }, 1000);
        }

        // Modal functions
        function openInvitationModal() {
            document.getElementById('invitationModal').classList.add('active');
        }

        function closeModal() {
            document.getElementById('invitationModal').classList.remove('active');
            document.getElementById('successMessage').style.display = 'none';
            document.getElementById('guestName').value = '';
            document.getElementById('guestCode').value = '';
        }

        // Download invitation function with enhanced design
        function downloadInvitation() {
            const name = document.getElementById('guestName').value.trim();
            const code = document.getElementById('guestCode').value.trim();
            
            if (!name || !code) {
                alert('Please enter both name and code');
                return;
            }
            
            if (code.length !== 4 || !/^\d{4}$/.test(code)) {
                alert('Please enter a valid 4-digit code');
                return;
            }
            
            // Check if guest exists in database
            const guest = guestDatabase[code];
            if (!guest) {
                alert('Invalid code. Please check your invitation code.');
                return;
            }
            
            // Verify name matches (case insensitive, flexible matching)
            const inputNameLower = name.toLowerCase().replace(/\s+/g, ' ');
            const dbNameLower = guest.name.toLowerCase().replace(/\s+/g, ' ');
            
            if (!inputNameLower.includes(dbNameLower.split(' ')[0]) && !dbNameLower.includes(inputNameLower.split(' ')[0])) {
                alert('Name does not match the invitation code.');
                return;
            }
            
            // Show success message
            document.getElementById('successMessage').style.display = 'block';
            
            // Create and download personalized invitation
            setTimeout(() => {
                createPersonalizedInvitation(name, code, guest.photo);
            }, 1000);
        }

        // Create personalized invitation with futuristic dark theme
        function createPersonalizedInvitation(name, code, photoUrl) {
            const canvas = document.createElement('canvas');
            const ctx = canvas.getContext('2d');
            canvas.width = 800;
            canvas.height = 1200;
            
            // Ultra dark gradient background
            const gradient = ctx.createLinearGradient(0, 0, 0, canvas.height);
            gradient.addColorStop(0, '#0a0a0f');
            gradient.addColorStop(0.3, '#121218');
            gradient.addColorStop(0.7, '#1a1a2e');
            gradient.addColorStop(1, '#0d0d1a');
            ctx.fillStyle = gradient;
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            // Add neon border with glow effect
            ctx.strokeStyle = '#00fff5';
            ctx.lineWidth = 8;
            ctx.shadowColor = '#00fff5';
            ctx.shadowBlur = 30;
            ctx.strokeRect(30, 30, canvas.width - 60, canvas.height - 60);
            
            // Inner border
            ctx.strokeStyle = '#bf00ff';
            ctx.lineWidth = 4;
            ctx.shadowColor = '#bf00ff';
            ctx.shadowBlur = 20;
            ctx.strokeRect(50, 50, canvas.width - 100, canvas.height - 100);
            ctx.shadowBlur = 0;
            
            // Title with neon gradient effect
            const titleGradient = ctx.createLinearGradient(0, 100, 0, 200);
            titleGradient.addColorStop(0, '#00fff5');
            titleGradient.addColorStop(0.5, '#bf00ff');
            titleGradient.addColorStop(1, '#ff0080');
            ctx.fillStyle = titleGradient;
            ctx.font = 'bold 54px Arial';
            ctx.textAlign = 'center';
            ctx.shadowColor = '#00fff5';
            ctx.shadowBlur = 20;
            ctx.fillText('FAREWELL', canvas.width / 2, 150);
            ctx.shadowBlur = 0;
            
            // Subtitle
            ctx.font = 'bold 36px Arial';
            ctx.fillStyle = '#ffffff';
            ctx.shadowColor = '#ffffff';
            ctx.shadowBlur = 15;
            ctx.fillText('CELEBRATION', canvas.width / 2, 200);
            ctx.shadowBlur = 0;
            
            // Guest name with enhanced glow
            ctx.font = 'bold 42px Arial';
            ctx.fillStyle = '#00fff5';
            ctx.shadowColor = '#00fff5';
            ctx.shadowBlur = 25;
            ctx.fillText(`${name}`, canvas.width / 2, 280);
            ctx.shadowBlur = 0;
            
            // Invitation text
            ctx.font = '28px Arial';
            ctx.fillStyle = '#ffffff';
            ctx.fillText('You are cordially invited', canvas.width / 2, 350);
            
            // Event details with neon highlights
            ctx.font = 'bold 32px Arial';
            ctx.fillStyle = '#00fff5';
            ctx.shadowColor = '#00fff5';
            ctx.shadowBlur = 15;
            ctx.fillText('29 NOVEMBER 2025', canvas.width / 2, 420);
            ctx.shadowBlur = 0;
            
            ctx.font = '24px Arial';
            ctx.fillStyle = '#ff0080';
            ctx.shadowColor = '#ff0080';
            ctx.shadowBlur = 10;
            ctx.fillText('Ashirwad Garden, Ratu, Ranchi', canvas.width / 2, 470);
            ctx.shadowBlur = 0;
            
            // Guest code with highlight box
            ctx.fillStyle = 'rgba(0, 255, 245, 0.2)';
            ctx.fillRect(canvas.width/2 - 120, 520, 240, 60);
            ctx.strokeStyle = '#00fff5';
            ctx.lineWidth = 2;
            ctx.strokeRect(canvas.width/2 - 120, 520, 240, 60);
            
            ctx.font = 'bold 24px Arial';
            ctx.fillStyle = '#00fff5';
            ctx.fillText(`CODE: ${code}`, canvas.width / 2, 555);
            
            // Additional details
            ctx.font = '22px Arial';
            ctx.fillStyle = '#ffffff';
            ctx.fillText('Dinner & Dancing to Follow', canvas.width / 2, 630);
            ctx.fillText('Dress Code: Semi-Formal', canvas.width / 2, 670);
            
            // Footer message
            ctx.font = '18px Arial';
            ctx.fillStyle = '#bf00ff';
            ctx.fillText('Looking forward to celebrating with you!', canvas.width / 2, 750);
            
            // Decorative neon elements
            const decorativeGradient = ctx.createRadialGradient(100, 100, 0, 100, 100, 30);
            decorativeGradient.addColorStop(0, '#00fff5');
            decorativeGradient.addColorStop(1, 'transparent');
            ctx.fillStyle = decorativeGradient;
            ctx.fillRect(70, 70, 60, 60);
            
            const decorativeGradient2 = ctx.createRadialGradient(700, 100, 0, 700, 100, 30);
            decorativeGradient2.addColorStop(0, '#ff0080');
            decorativeGradient2.addColorStop(1, 'transparent');
            ctx.fillStyle = decorativeGradient2;
            ctx.fillRect(670, 70, 60, 60);
            
            // Convert to blob and download
            canvas.toBlob(function(blob) {
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = `Neon_Farewell_Invitation_${name.replace(/\s+/g, '_')}_${code}.png`;
                document.body.appendChild(a);
                a.click();
                document.body.removeChild(a);
                URL.revokeObjectURL(url);
                
                setTimeout(closeModal, 2000);
            });
        }

        // Background music
        document.addEventListener('click', function() {
            const audio = document.getElementById('backgroundMusic');
            if (audio.paused) {
                audio.play().catch(e => console.log('Audio play failed:', e));
            }
        }, { once: true });

        // Input validation for guest code
        document.getElementById('guestCode').addEventListener('input', function(e) {
            this.value = this.value.replace(/[^0-9]/g, '');
        });

        // Enter key support for form
        document.getElementById('guestCode').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                downloadInvitation();
            }
        });

        document.getElementById('guestName').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                document.getElementById('guestCode').focus();
            }
        });

        // Close modal on background click
        document.getElementById('invitationModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeModal();
            }
        });

        // Enhanced interactive effects for countdown
        document.addEventListener('DOMContentLoaded', function() {
            // Add hover effects after countdown is created
            setTimeout(() => {
                const countdownItems = document.querySelectorAll('.countdown-item');
                countdownItems.forEach((item, index) => {
                    item.addEventListener('mouseenter', function() {
                        this.style.transform = 'translateY(-15px) rotateY(10deg) scale(1.05)';
                        this.style.transition = 'all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
                    });
                    
                    item.addEventListener('mouseleave', function() {
                        this.style.transform = 'translateY(0) rotateY(0) scale(1)';
                    });
                });
            }, 3000);
        });

        // Console messages with neon theme
        console.log('%cFUTURISTIC FAREWELL CELEBRATION%c\n\nYour presence is requested!', 
            'color: #00fff5; font-weight: bold; font-size: 28px; text-shadow: 0 0 15px #00fff5; background: #0a0a0f; padding: 10px;', 
            'color: #bf00ff; font-weight: bold; font-size: 18px; text-shadow: 0 0 10px #bf00ff;');

        console.log('%cNeon-powered invitation system activated! 🚀', 
            'color: #ff0080; background: linear-gradient(45deg, #0a0a0f, #1a1a2e); font-size: 20pt; font-weight: bold; padding: 15px; border-radius: 10px; border: 2px solid #00fff5;');
    </script>
</body>
</html>